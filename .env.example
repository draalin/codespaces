# Copy to .env (which is gitignored) and adjust values.
# Load manually: export $(grep -v '^#' .env | xargs)

# AWS / Terraform
AWS_PROFILE=example-sso-admin-profile
AWS_REGION=us-east-1

# Infracost
INFRACOST_API_KEY=replace_me

# Anthropic Claude (if using direct API calls in scripts)
ANTHROPIC_API_KEY=replace_me

# OpenAI (if you add an OpenAI extension or custom tooling)
OPENAI_API_KEY=replace_me

# Optional: Terraform Cloud / Enterprise
TERRAFORM_CLOUD_TOKEN=replace_me

# Any custom variables for modules
PROJECT_TAG=demo
